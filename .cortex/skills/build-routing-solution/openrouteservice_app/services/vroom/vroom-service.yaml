spec:
  containers:
    - name: vroom
      image: /openrouteservice_app/core/image_repository/vroom-docker:v1.0.4
      env:
        VROOM_ROUTER: ors
  endpoints:
    - name: vroom
      port: 3000
      public: false

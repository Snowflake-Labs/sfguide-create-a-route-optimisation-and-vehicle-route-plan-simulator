# Deployment Log: deploy-all.sql

**Date:** 2026-05-29 16:39 UTC
**Skill:** deploy-all (full stack deployment)
**Outcome:** SUCCESS (with 1 non-critical error)

## Issues

### 1. deploy-agent.sql line 499 — ALTER SNOWFLAKE INTELLIGENCE fails

**Module:** `.cortex/skills/routing-agent/references/deploy-agent.sql`
**Error:**
```
SQL compilation error: Snowflake Intelligence 'SNOWFLAKE_INTELLIGENCE_OBJECT_DEFAULT' does not exist or not authorized.
```
**Impact:** Non-critical. All 9 tool procedures (TOOL_DIRECTIONS, TOOL_ISOCHRONE, TOOL_OPTIMIZATION, TOOL_WEATHER, TOOL_SUPPLY_CHAIN, TOOL_INVENTORY_STATUS, TOOL_DEMAND_FORECAST, TOOL_REPLENISHMENT_PLAN, TOOL_PLANT_IMPACT) were created successfully before this line. The ROUTING_AGENT was subsequently created by `configure-agent.sql` in Phase 5.

**Resolution:** Skipped. The `ALTER SNOWFLAKE INTELLIGENCE` statement attempts to register the agent with an account-level Snowflake Intelligence object that doesn't exist. Agent functions correctly without this.

## Phase Results

| Phase | Description | Status |
|-------|-------------|--------|
| 0 | Warehouse setup (ROUTING_DEPLOY LARGE, ROUTING_ANALYTICS XS) | OK |
| 1 | Core infrastructure (modules 00-06, REGION_ORS_MAP seed) | OK |
| — | Wait for 5 SPCS services to reach RUNNING | OK (all 5 RUNNING immediately) |
| 2 | Seed data (parquet upload + load-seed-data.sql) | OK |
| 3 | Demo skills (taxis, food-delivery, route-deviation, dwell, optimization, retail) | OK |
| 4 | Agent tools & intelligence (demo-data, weather, pharma, plant-map, fleet-analytics, agent procs, pharma-catchment) | OK (1 non-critical error) |
| 5 | Agent + Streamlit (semantic view, configure-agent, Fleet Explorer, agent-demos.json) | OK |
| 6 | Cleanup (drop ROUTING_DEPLOY warehouse) | OK |

## Verification

| Check | Expected | Actual |
|-------|----------|--------|
| Services RUNNING | 5 | 5 |
| FACT_TRIPS rows | 6008 | 6008 |
| ROUTE_OPTIMIZATION.PLACES rows | ~1.4M | 1,438,458 |
| ROUTING_AGENT agent | exists | exists |
| FLEET_MAP streamlit | exists | exists |

## Notes

- Service specs must be uploaded to `@ORS_SPCS_STAGE/services/<service-name>/` before module 01 runs. The bulk workspace upload places them under `deploy/...` which CREATE SERVICE can't find.
- Stray `ors-config.yml` files were removed from `deploy/` paths and uploaded correctly to `@ORS_SPCS_STAGE/SanFrancisco/ors-config.yml` to prevent phantom region creation.
- All `EXECUTE IMMEDIATE FROM` statements were executed via `snow sql -q` (Bash tool) per deploy rule 8, avoiding the jsonv2 format error from the Cortex Code SQL tool.

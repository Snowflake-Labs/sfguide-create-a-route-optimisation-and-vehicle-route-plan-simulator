# Friction Log — harden-downsize-prewarm — 2026-06-02 14:39

Session type: source edit + SQL-module redeploy (NOT a full build-routing-solution run).
Account: tib85385 (conn=TIB). Branch: feat/sfc-gh-obielov-feat.

## Goal
Harden the level-driven downsize + MMAP prewarm path (commits 4a9152d/bcb53cb/13fc073):
1. Make swallowed downsize failures observable (post-recreate readiness assertion + COST_GUARD_LOG + DEGRADED: return prefix).
2. Decouple resume-path latency: replace synchronous prewarm with a NEEDS_PREWARM flag drained by the existing */2 min RESCUE_PENDING_PROVISIONS reconciler, wake-safe via a passive SHOW SERVICES status gate.

## Steps & status
| Step | Detail | Status | Notes |
|---|---|---|---|
| 1 | DOWNSIZE readiness assertion + COST_GUARD_LOG + gate prewarm on ok | OK | additive, non-throwing |
| 2 | Capture downsize result at wrapper / FINALIZE_PROVISION_ITER / FINALIZE_DEFAULT_REGION_IF_READY | OK | ERROR_MSG annotation on DEGRADED/Refusing |
| 3 | NEEDS_PREWARM column (02 + 03 CREATE blocks + EXECUTE IMMEDIATE-wrapped backfill) | OK | mirrors GRAPHS_DATA_ACCESS backfill |
| 4 | Setters flip flag: resume_region_ors, APPLY_ORS_LIMITS, RESUME_ALL_SERVICES (set-based), DOWNSIZE (set+clear) | OK | |
| 5 | Reconciler drain loop in RESCUE_PENDING_PROVISIONS (SHOW SERVICES RUNNING gate) + clear-on-suspend | OK | placed after REPAIR, before RETURN |
| 6 | Redeploy modules 02/03/04 + live validate | OK | see validation |
| 7 | Commit + push + friction log | OK | |

## Validation (live, tib85385)
- Module deploys: 02 OK, 03 OK (35 objects, no exec errors), 04 OK (10 objects). exit=0 each.
- Column: REGION_ORS_MAP.NEEDS_PREWARM present; Europe=MMAP/CPU_X64_SL, SanFrancisco=RAM_STORE/GEN_X64_G2_4.
- Test A (drain): set Europe NEEDS_PREWARM=TRUE -> CALL RESCUE_PENDING_PROVISIONS (ran ~71s incl. prewarm sweep) -> flag cleared to FALSE. PASS.
- Test C (RESUME_ALL_SERVICES set-based): call flipped Europe (MMAP)=TRUE, SanFrancisco (RAM_STORE) stayed FALSE. PASS.
- Test B (wake-safe gate): SHOW SERVICES LIKE absent service -> 0 rows -> pw_status stays '' -> skip (no probe, no wake). PASS. COST_GUARD_LOG queryable (empty).
- Cleared test-induced flag afterward (both regions FALSE).

## Friction points
- F1: snow CLI echoes the full SQL source, so grepping deploy output for "error"/"fail" matches comments/column names (e.g. ERROR_MSG, "failure"), not real errors. Resolution: grep for exec-error patterns (`^Error`, SQLSTATE, Traceback) and count "successfully created" + exit code instead. Recommendation: a deploy helper that parses only snow CLI status boxes would remove this ambiguity (known run_sql_module.sh box-detection gap).
- F2: Change 1 (DOWNSIZE readiness assertion) NOT live-triggered — a real re-downsize recreates the running Europe service (~5 min disruption). Validated by clean compile + reachable COST_GUARD_LOG + reuse of the proven canary pattern. Recommendation: exercise the DEGRADED path during the next genuine country/continent build rather than forcing a disruptive re-downsize.

## Outcome
All 7 tasks complete. Latency-decoupling and failure-observability verified live without disrupting running services. No regressions observed.

# Deployment Log: deploy-all.sql

**Date:** 2026-05-29 13:20 UTC
**Skill:** deploy-all (full stack deployment)
**Outcome:** SUCCESS (with 1 non-critical error)

## Issues

### 1. deploy-agent.sql line 499 — ALTER SNOWFLAKE INTELLIGENCE fails

**Module:** `.cortex/skills/routing-agent/references/deploy-agent.sql`
**Error:**
```
SQL compilation error: Snowflake Intelligence 'SNOWFLAKE_INTELLIGENCE_OBJECT_DEFAULT' does not exist or not authorized.
```
**Impact:** Non-critical. The agent was already created by `configure-agent.sql`. This ALTER statement attempts to register the agent with a Snowflake Intelligence object that doesn't exist in this account. All 8 tool procedures and the ROUTING_AGENT itself were created successfully before this line.

**Resolution:** Skipped. Agent works correctly without this registration.

## Phase Results

| Phase | Description | Status |
|-------|-------------|--------|
| 0 | Warehouse setup (ROUTING_DEPLOY LARGE, ROUTING_ANALYTICS XS) | OK |
| 1 | Core infrastructure (modules 00-06, REGION_ORS_MAP seed) | OK |
| 2 | Seed data (parquet upload + load-seed-data.sql) | OK |
| — | Wait for 5 SPCS services to reach RUNNING | OK |
| 3 | Demo skills (taxis, food-delivery, route-deviation, dwell, optimization, retail) | OK |
| 4 | Agent tools & intelligence (demo-data, weather, pharma, plant-map, fleet-analytics) | OK |
| 5 | Agent + Streamlit (semantic view, configure-agent, Fleet Explorer, agent-demos.json) | OK |
| 6 | Cleanup (drop ROUTING_DEPLOY warehouse) | OK |

## Verification

| Check | Expected | Actual |
|-------|----------|--------|
| Services RUNNING | 5 | 5 |
| FACT_TRIPS rows | 6008 | 6008 |
| ROUTE_OPTIMIZATION.PLACES rows | ~1.4M | 1,438,458 |
| ROUTING_AGENT agent | exists | exists |
| FLEET_MAP streamlit | exists | exists |
| TOOL_WEATHER callable | SUCCESS | SUCCESS |
| Control app endpoint | accessible | https://isddrob-sfsehol-summit-26-bi220-june-1-itchbp.snowflakecomputing.app |

## Notes

- The `EXECUTE IMMEDIATE FROM` statements return HTTP 400 (code 391917) when the script produces output that doesn't fit jsonv2 format. This is cosmetic — the scripts execute successfully. Verified by checking created objects after each module.
- Service specs must be uploaded to `@ORS_SPCS_STAGE/services/<service-name>/` (not under `deploy/`). The bulk workspace upload places them in `deploy/...` path which CREATE SERVICE can't find.
- ROUTING_AGENT schema must be created before running `deploy-demo-data.sql` which creates procedures in that schema.

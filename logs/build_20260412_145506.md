# Build Routing Solution Log — 2026-04-12 14:55:06

## Friction, Confusion, Bugs & Observations

### 14:55 — Snow CLI version warning
- `snow` CLI is v3.9.0 but v3.16.0 is available. Not a blocker but may cause issues with newer features.
- SKILL.md Step 2 says "Set the container command variable" but doesn't explain HOW to persist it across bash calls. Each bash call is a fresh session, so CONTAINER_CMD won't persist. This is a minor clarity issue for the skill doc — agent just needs to remember which to use.

### 14:57 — Snow CLI `image-list` command wrong
- `references/build-images.md` line 79 says `snow spcs image-list ...` but the correct command is `snow spcs image-repository list-images ...`. The `image-list` subcommand doesn't exist in snow CLI v3.9.0. **BUG in build-images.md.**

### 15:10 — npm audit vulnerabilities (expected)
- `npm ci` for ors_control_app reported 4 vulnerabilities (2 moderate, 2 high). Skill doc correctly notes "These are in dev/build dependencies and do not affect the runtime container." Not a real issue.

### 15:30 — Step 8 seed data loaded perfectly on first try
- All counts matched expected values exactly. No metadata caching issues encountered.

### 15:32 — Step 8b Overture Maps install was smooth
- Both SYSTEM$ACCEPT_LEGAL_TERMS calls worked fine. Databases created successfully.

### 15:33 — Starting demo deployments (all 6 selected)
- Deploying: Food Delivery, Route Deviation, Dwell Analysis, Fleet Taxis, Retail Catchment, Route Optimization

### 15:35 — Food Delivery: smooth
- `snow sql -f` for seed-data.sql worked perfectly. CONFIG=1, DELIVERIES=6008, RESTAURANTS_ENRICHED=5000.

### 15:37 — Route Deviation: smooth
- TRIP_DEVIATION_ANALYSIS=6008, DRIVER_DEVIATION_SUMMARY=50, DAILY_DEVIATION_TRENDS=8.

### 15:40 — Dwell Analysis: DT_SLA_ALERTS = 0
- All other DTs populated but DT_SLA_ALERTS has 0 rows. The default SLA thresholds (60/120 min for WAREHOUSE, etc.) are likely too high for the synthetic data's dwell durations. Not a bug per se, but might confuse first-time users. **Suggestion:** Consider adding more aggressive sample thresholds for demo purposes, or document that seed data may not trigger SLA alerts.

### 15:42 — Fleet Taxis: smooth via seed-data.sql
- All wrapper views populated. Used projection view path (Step 0) since we have seed data.

### 15:45 — Retail Catchment: required temporary SQL file
- Skill doc Step 5 uses SET session variables that don't persist across `snowflake_sql_execute` calls. The doc correctly notes to use `snow sql -f`. Had to create a temp SQL file to run the whole thing in one session. **Friction:** No pre-built combined SQL file in `references/` — the agent has to assemble it from `sql-pipeline.md` fragments. Consider providing a `seed-data.sql` like other skills.
- RETAIL_POIS=56,303, CITIES_BY_STATE=138, REGIONAL_ADDRESSES=2,826,892.

### 15:50 — Route Optimization: PLACES count = 1,430,684
- The geohash '9q' covers a huge area (entire SF Bay Area region), hence 1.4M POIs instead of expected 50K-500K. The notebook-deployment.md says "typically 50K-500K rows depending on region" but this exceeds that. Not a bug but doc should update the expected range. The PLACES table was created via CTAS from within the notebook, using a 2-character geohash filter. Consider using region bounding box for filtering.
- LOOKUP=4 (expected 3+ ✓), JOB_TEMPLATE=29 (✓).

### 15:52 — Summary of friction points
1. **BUG:** `references/build-images.md` line 79: `snow spcs image-list` should be `snow spcs image-repository list-images`
2. **CLARITY:** SKILL.md Step 2 says "Set CONTAINER_CMD variable" but doesn't address that bash sessions don't persist variables
3. **MISSING:** Retail Catchment has no `seed-data.sql` combined file unlike other skills — requires agent to assemble SQL from fragments
4. **DOC:** PLACES row count range "50K-500K" in notebook-deployment.md is too low for '9q' geohash (got 1.4M)
5. **UX:** DT_SLA_ALERTS = 0 with default thresholds on seed data may confuse first-time users
6. **MINOR:** Snow CLI v3.9.0 warns about v3.16.0 on every command (noisy but not blocking)

